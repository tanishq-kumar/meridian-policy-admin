spring:
  datasource:
    url: jdbc:tc:postgresql:17-alpine:///meridian_test
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    locations: classpath:db/migration
    schemas: pas
meridian:
  pii:
    key: test-key-1234567890
  events:
    enabled: false
